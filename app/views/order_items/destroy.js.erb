$(".cart-text").html("<%= escape_javascript(render 'layouts/cart_text') %>")
$(".shopping-cart").html("<%= escape_javascript(render 'orders/shopping_cart') %>")

<%# returns some js that is used to manipulate the cart when an item is removed %>